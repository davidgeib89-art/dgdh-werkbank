commands:
  install: pnpm install
  typecheck: pnpm -r typecheck
  build: pnpm build
  test: pnpm test:run
  lint: "echo 'no root lint script'"
  build_cli: pnpm --filter paperclipai build

services: {}
